{
  "name": "merchcube",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "postcss src/index.css -o assets/tailwind.css",
    "start": "npm run build && theme deploy && theme watch",
    "init_builder": "NODE_ENV=production npm run build && theme deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.0",
    "postcss-cli": "^9.0.2",
    "tailwindcss": "^2.2.19"
  }
}
